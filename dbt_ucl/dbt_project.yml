name: "ucl_analytics"
version: "1.0.0"
config-version: 2

profile: "ucl_duckdb"

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  ucl_analytics:
    staging:
      +materialized: "view"
      +schema: "staging"

    marts:
      +materialized: "table"
      +schema: "marts"
